# Plan critic — round 9

## Winner

Plan. The critical path is executable, hermetic and acyclic.

## Hardening challenge before execution

Phase -3 inventoried refs/worktrees but not fleet records. Five or more
nonterminal Sergeant records have dead pane identities and own branches later
phases would adopt. `add-cross-platform-serge-4dfdd3` remains `needs_input`.

Pane numbers are not identities after a tmux restart. `%2117`, recorded as the
Sergeant coordinator, no longer exists; `%21` was observed reused by an unrelated
shell. Ownership needs exact pane identity plus process start and durable
handover.

## Exact challenge

Add one inventory row per fleet record. Fail until every nonterminal record has
a valid response, wake condition or audited handover. Mutate a dead record to a
live foreign pane number with the old process start; reconciliation must fail as
reused. Do not use fleet-wide sync to settle human decision states.
