# Plan critic — round 31

## Winner

Quality bar. Trust was assigned to a remote containing mixed histories.

## Largest gap

Two no-mistakes refs had no merge-base with Sergeant and contained a dashboard
project tree. Existing schema required a merge-base and would deadlock inventory,
overlap and disposition while reporting 168 apparent Sergeant deletions.

## Exact challenge

Classify trust per ref. Preserve unrelated commits with tree/provenance digest,
but exclude them from Sergeant overlap/rebase/merge. The path set must name seven
refs, exclude two foreign-history refs, and require ownership for five Sergeant
refs. Misclassifying foreign history must fail closed.
