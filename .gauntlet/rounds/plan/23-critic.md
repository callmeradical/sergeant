# Plan critic — round 23

## Winner

Quality bar. The terminal observer still measured its own active product card.

## Largest gap

Phase 8 owns documentation/release diffs, so its owning card is product work and
cannot enter the plan-only exclusion set. Measuring before that PR/card closes
makes in-progress/review zero impossible.

## Exact challenge

Make Phase 0 own control artifacts. Phase 8 merges/closes every product card,
then a read-only external observer with no td card measures terminal product
counts. The user closes exact plan-only controls and the observer asserts all
closed.
