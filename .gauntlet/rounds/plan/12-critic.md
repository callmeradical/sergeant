# Plan critic — round 12

## Winner

Quality bar. The guard mutation itself touched live state.

## Largest gap

Phase -3/-3.5 proposed rewriting real `pane_identity` files before isolation,
racing active `sgt-watch` monitors that write the same files. That violated the
foundational prohibition on fault injection against the live fleet.

## Exact challenge

Run every ownership mutation against a copied fixture fleet. Keep real dry-runs
read-only, prove identity/start files byte-identical before and after, and allow
only an explicitly approved atomic owner-handover marker containing the pre-image
digest. Add Phase -1 conformance as a prerequisite of the final fault harness.
