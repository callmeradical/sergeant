# dashboard-shows-delivery-history-and-quarantine

R5.6 (partial) — the embedded dashboard and API expose a run's delivery history, including dead letters, and let an operator quarantine one with a reason
