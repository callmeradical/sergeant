# captured-output-is-redacted-and-bounded

R4.4, R4.5 (partial) — captured agent output is scrubbed for common secret shapes and size-capped before it is ever written to the store, closing the leak at the point content is captured rather than at the point it is read
