# dispatch-briefs-render-from-the-stored-intent

Both ways an agent gets its work — dispatched through the UI or asking over MCP — render the same brief from the stored Intent and Bullet rows, instead of one getting the operator's raw typed text and the other getting a dump of unrelated project config.
