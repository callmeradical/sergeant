# embedded-terminal

Adds a real, interactive terminal to the dashboard — a Go-spawned PTY bridged to the browser over WebSocket, with an xterm.js frontend and multiple named sessions — so an operator can act directly on a worktree without leaving the browser. Extends D8 with a complementary control surface; does not change what D8 governs.
