# graphify-is-a-native-v2-capability

D9 (Task 1) — v2's own Go code builds and publishes a project's cross-repository code graph, and exposes it to agents over MCP as query/explain/affected — without shelling out to v1's `sgt-graphify`
